# Places UI

POIs and libraries are created and managed in the Places database by using the Places UI. 

* To create or modify a library, see [Manage libraries in the Places UI](manage-libraries).
* To create or modify a POI, see [Manage POIs in the Places UI](managing-pois-in-the-places-ui).
* To preform a bulk import of POIs, see [Bulk import of POIs with a script](bulk-import-of-pois).

## 









